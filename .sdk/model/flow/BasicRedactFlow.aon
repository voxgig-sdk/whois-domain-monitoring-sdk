# BasicRedactFlow

main: kit: flow: BasicRedactFlow:
{
  entity: "redact",
  kind: "basic",
  name: "BasicRedactFlow",
  step: [
    {
      input: {
        ref: "redact_ref01"
      }

      op: "create"
    }
  ],
  "key$": "BasicRedactFlow"
}